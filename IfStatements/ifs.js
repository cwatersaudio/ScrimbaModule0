// if (5 > 3) {
//     console.log("is greater than")
// }

var cat = "cat"

// if (cat.length === 3) {
//     console.log("is the length")
// } else {
//     console.log("it's not the length")
// }
var dog = "cat"

// if (cat == dog) {
//     console.log("they're the same")
// } else if (cat.length === dog.length) {
//     console.log("but they've got the same number of letters")
// } else {
//     console.log("they're not the same")
// }

var person = {            //<-- this is used for the next two codes
    name: "Ballysen",
    age: "21",
  }

//   if (person.age > 18) {
//         console.log(person.name + " is allowed to go to the movie")
//   } else if (person.age == NaN) {
//     console.log(person.name + " doesn't seem to have an age")  //cpuldnt' get this to work
//   } else { 
//     console.log(person.name + " is too young for this film")
//   }

//   if (person.name [0] === "B" && person.age >= 18) {
//     console.log(person.name + " is allowed to go to the movie")
// } else if (person.name [0] != "B" && person.age < 18)  {
//     console.log(person.name + " is too young for this film because their name begins with " + person.name [0] +" and not B AND they're only " + person.age +" years old.") 
// } else if (person.name [0] != "B")  { 
// console.log(person.name + " is too young for this film because their name begins with " + person.name [0] +" and not B")
// } else if (person.age < 18) {
//     console.log(person.name + " is too young for this film because they're only " + person.age +" years old.")
// } else {
//     console.log("Not sure what's happening.")

// }

// var dog = 3;

// if (typeof dog === "string") {
//     console.log("That's a string!")
// } else {
//     console.log("That's not a string!")

// }

// var truth = 5;

// if (typeof truth === "boolean") {
//     console.log("That's a Boolean!")
// } else {
//     console.log("That's not a Boolean!")

// }

// let result =
//     (typeof truth ==="boolean") ?  "That's a Boolean!" : "That's not a boolean!";

//     console.log(result);

// var myNum = 7;

// let result = (myNum % 2 === 0) ? "That's an even number right there!" : "That's not even I don't think...";

// console.log("Here's the result of the test: " + result)

